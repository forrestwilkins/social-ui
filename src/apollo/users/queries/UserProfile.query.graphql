query UserProfile($name: String) {
  user(name: $name) {
    ...UserProfileCard

    profileFeed {
      ... on Post {
        ...PostCard
      }
      ... on Proposal {
        ...ProposalCard
      }
    }
  }
}
