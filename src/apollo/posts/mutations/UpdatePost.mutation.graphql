mutation UpdatePost($postData: UpdatePostInput!) {
  updatePost(postData: $postData) {
    post {
      id
      body
      user {
        ...UserAvatar
      }
      group {
        ...GroupAvatar
      }
      createdAt
    }
  }
}
