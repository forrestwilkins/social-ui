mutation CreateProposal($proposalData: CreateProposalInput!) {
  createProposal(proposalData: $proposalData) {
    proposal {
      id
      body
      createdAt

      user {
        id
      }
      group {
        id
      }
    }
  }
}
