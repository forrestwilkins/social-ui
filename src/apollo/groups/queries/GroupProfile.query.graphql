query GroupProfile($name: String!) {
  group(name: $name) {
    ...GroupProfileCard

    feed {
      ... on Post {
        ...PostCard
      }
      ... on Proposal {
        ...ProposalCard
      }
    }
  }

  me {
    id
  }
}
