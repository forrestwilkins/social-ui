version: "3.8"

services:
  social-ui-dev:
    build:
      context: ../../
      dockerfile: docker/social-ui-dev/Dockerfile
    container_name: social-ui-dev
    image: social-ui-dev
    volumes:
      - ./:/app
      - /app/node_modules
      - /app/.next
    ports:
      - ${PORT}:${PORT}
