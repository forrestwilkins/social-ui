mutation CreatePost($postData: CreatePostInput!) {
  createPost(postData: $postData) {
    post {
      id
      body
      user {
        ...UserAvatar
      }
      group {
        ...GroupAvatar
      }
      createdAt
    }
  }
}
